- when using ssh the terminal name color should change, and a border around the terminal should appear
- make terminal name modifiable
- The text "PROJECTS" its in the wrong place it shouldnt be over "STANDALONE TERINALS"
- when doing cmd + + an ungly scrolling bar appears to the right.
- add a way to accepts from cli when opening (or focusing) the program, so that the project with the argument passed can be focused. example open '/Applications/Minty.app' -p project1 should open Minty with project1 focused (it should be focused on the last open terminal if there is any. 
